```
git remote add upstream https://github.com/mehmetoguzderin/hashcode-2020-tayga.git
git fetch upstream
git fetch origin
git checkout upstream/master
git branch -d master
git checkout -b master upstream/master
git pull origin master
git push -u origin master
```

```
git remote add upstream https://github.com/mehmetoguzderin/hashcode-2020-tayga.git ; git fetch upstream ; git fetch origin ; git checkout upstream/master ; git branch -d master ; git checkout -b master upstream/master ; git pull origin master ; git push -u origin master
```
